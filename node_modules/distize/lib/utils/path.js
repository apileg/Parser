"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.glob = glob;
var _glob = _interopRequireDefault(require("glob"));
function _interopRequireDefault(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function glob(pattern1) {
    var options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var patterns = Array.isArray(pattern1) ? pattern1 : [
        pattern1
    ];
    return Promise.all(patterns.map(function(pattern) {
        return new Promise(function(resolve, reject) {
            return (0, _glob).default(pattern, options, function(err, matches) {
                return err ? reject(err) : resolve(matches);
            });
        });
    })).then(function(matches) {
        return matches.reduce(function(carry, match) {
            return carry.concat(match);
        });
    });
}

//# sourceMappingURL=path.js.map